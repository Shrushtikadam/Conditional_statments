#include<stdio.h>

int main(void)
{
    int m;
    int n;
    int and_result;
    int or_result;


    printf("enter m:");
    scanf("%d", &m);

    printf("Enter n:");
    scanf("%d", &n);

    result = (n>0) && (n>0);

    and_result =(m>0) && (n>0);
    or_result=(n>0) || (n>0);

    printf("and result=%d, or_result=%d\n,and_result,or_result");
    return(0);
    }

    